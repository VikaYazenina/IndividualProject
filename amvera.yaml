meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11

build:
  requirementsPath: requirements.txt

run:
  scriptName: run.py
  containerPort: 80
  zeroDowntime: true
  singleton: false

